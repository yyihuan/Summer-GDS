# Via 功能失效调查 - 简明实施指南

## 📋 核心问题
- **问题**：Via/刻孔功能在提交 03c3e9d 后失效
- **原因猜测**：全局精度处理破坏了布尔减法
- **验证方法**：通过日志链路追踪

## 🎯 关键验证点

### Point 1: 布尔运算失效检查 ⭐ 最重要
**文件**：`gds_utils/region.py`
**方法**：`polygon2ring()` 第204行附近

**添加代码**：
```python
result = outer_region - inner_region
result_kdbregion = result.get_klayout_region()

# ⭐ 这行是关键
if result_kdbregion.is_empty():
    logger.error(f"❌ VIA失效: 布尔减法结果为空！outer={outer_kdbregion.polygons()}, inner={inner_kdbregion.polygons()}")
else:
    logger.info(f"✓ VIA成功: 布尔减法得到{result_kdbregion.polygons()}个多边形")
```

**检查方式**：
```bash
python main.py test_via.yaml 2>&1 | grep "VIA"
```

---

### Point 2: 精度差异检查 ⭐⭐ 次重要
**文件**：`gds_utils/gds.py`
**方法**：`apply_global_precision()` 内部

**添加代码**：
```python
# 在处理完坐标后加入
if old_points[i].x != new_points[i].x or old_points[i].y != new_points[i].y:
    logger.warning(f"[精度改变] 顶点{i}: ({old_points[i].x},{old_points[i].y}) → ({new_points[i].x},{new_points[i].y})")
```

**检查方式**：
```bash
python main.py test_via.yaml 2>&1 | grep "精度改变" | wc -l
```

---

## 📊 验证矩阵

| 检查项 | 预期（正常） | 预期（损坏） | 实际 |
|--------|------------|-----------|------|
| VIA布尔结果 | ✓ 成功 | ❌ 失效 | ? |
| 精度处理执行 | 无 | 有 | ? |
| 精度导致改变 | 否 | 是 | ? |

---

## ✅ 验证完成标志

**损坏版本**：
```
❌ VIA失效: 布尔减法结果为空！
[精度改变] 顶点0: (10000,20000) → (10001,20000)
```

**正常版本**：
```
✓ VIA成功: 布尔减法得到1个多边形
(没有 [精度改变] 日志)
```

**结论判断**：如果两个版本的日志完全相反，则假设成立 ✅
